{
  "name": "@namefix/menu-bar",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix ../.. run build && vite",
    "build": "npm --prefix ../.. run build && vite build",
    "preview": "vite preview",
    "tauri:dev": "npm --prefix ../.. run build && tauri dev",
    "tauri:build": "npm --prefix ../.. run build && tauri build --bundles app,dmg"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.1",
    "@tauri-apps/plugin-autostart": "^2.5.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.1",
    "@types/node": "^20.12.12",
    "typescript": "^5.5.0",
    "vite": "^5.2.0"
  }
}
